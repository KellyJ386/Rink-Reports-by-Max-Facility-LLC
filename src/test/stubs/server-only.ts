// Test stub for the "server-only" package. The real package throws
// at import time when bundled into a client environment. In Vitest
// jsdom we don't care about that constraint, so this stub is empty.
export {};
